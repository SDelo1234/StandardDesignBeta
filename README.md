# Heras Fencing – Codex Project

This repo is ready for Vercel (Vite + React + Tailwind).

## Replace the UI
Overwrite `src/Heras-fence-ui.jsx` with your full component (keep the filename and casing).

## Run locally
npm install
npm run dev

## Deploy on Vercel
Framework: Vite
Install: npm install
Build: npm run build
Output: dist
Node: 18 or 20